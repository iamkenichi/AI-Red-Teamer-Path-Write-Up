Task

- Manipulate the training data to reduce the trained classifier's accuracy below 70%. Submit the flag you obtain after providing a dataset that satisfies the lab requirements.

- Your goal is to manipulate a training data set to reduce accuracy of the trained classifier. You should provide the manipulated training data set in a CSV file. A classifier will automatically be trained and evaluated on your training data set.

Manipulate and upload the training data set in such a way that the trained classifier's accuracy is below 70%.

Download the original training data set.
